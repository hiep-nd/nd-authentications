# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

workspace 'NDAuthentications'

target 'Samples' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Samples
  pod 'NDAuthentications/Swift', :path => '.'
  pod 'NDModificationOperators_Swift'
  pod 'NDMVVM', :path => '../NDMVVM'
  pod 'NDUtils/objc', :path => '../NDUtils'
  pod 'NDUtils/Foundation', :path => '../NDUtils'
  pod 'NDUtils/UIKit', :path => '../NDUtils'
  
  target 'SamplesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SamplesUITests' do
    # Pods for testing
  end

end
